# prep-javascript-functions-quiz-notes


## Quiz Questions

After completing this exercise, you should be able to discuss or answer the following questions:

- What are the five parts of a function definition?
1.the function keyword to begin the a new function
2.give the function a name
3.next are the parameters within parentheses
4.starting a function code block with opening curly brackets
5.ending a function code block with closing curly brackets
- How do you call a function?
Must be called in within its code block to run
- What is the difference between a parameter and an argument?
When defining a function we delcare a parameters.  With an arguement we call the function and pass it as an arugement.
## Notes

All student notes should be written here.


How to write `Code Examples` in markdown

for JS:
```javascript
const data = "Howdy"
```

for HTML:
```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:
```css
div {
  width:100%
}
```
