# prep-javascript-variables-quiz-notes

## Quiz Questions

After completing this exercise, you should be able to discuss or answer the following questions:

- What does the `=` (equals) operator do in JavaScript?
The '=' is an assignment operator

- What operator will return the data type of a variable value?
The typeof operator

- Which keyword is used to declare a variable in JavaScript?
Can delcare with let,var,const

- Which characters is a JavaScript variable allowed to begin with?
The dollar sign ($), underscore(_), and letters

- What are quotation marks used for in JavaScript?
They are used for text strings

- What is the purpose of strings in JavaScript?
Is for storing and manipulating text

- What is the purpose of booleans in JavaScript?
To now if a data type of a value is true or false

- What is the purpose of numbers in JavaScript?
It is used to represent numeric values

- What does `null` mean in JavaScript?
It is something that doesn't exist, but is still considered an object.  It should be 'null'.

## Notes

All student notes should be written here.


How to write `Code Examples` in markdown

for JS:
```javascript
const data = "Howdy"
```

for HTML:
```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:
```css
div {
  width:100%
}
```
