# prep-javascript-objects-quiz-notes

## Quiz Questions

After completing this exercise, you should be able to discuss or answer the following questions:

- What data types can object properties hold?
They can hold strings, numbers, booleans, null, undefined, objects, arrays and functions.
- Describe the syntax (structure) of object-literals in JavaScript.
Starts with creating a object then opening with curly brackets({). Add properites and giving them a value.
## Notes

All student notes should be written here.

.notation can create properties and acess properties and braketnotation
bracket notation can creat illegal variable names
bracket notation can sub of variables
Reference data types
How to write `Code Examples` in markdown

for JS:
```javascript
const data = "Howdy"
```

for HTML:
```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:
```css
div {
  width:100%
}
```
